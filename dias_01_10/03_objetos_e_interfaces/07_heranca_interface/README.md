# 📄 Descrição do desafio - Herança de interface

Crie:
- interface `Animal` com: 
  - nome 
  - idade

- interface `Cachorro` que`: 
  - herda de `Animal` e 
  - adiciona `latir(): void`

Crie um objeto que representa um cachorro e chame `latir()`.